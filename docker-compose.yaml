version: '3.8'
volumes:
  data:
services:
  database:
    image: postgres
    ports:
     - 5432:5432
    environment:
     - POSTGRES_PASSWORD= ''
    volumes:
     - data:/var/lib/postgresql/data


 